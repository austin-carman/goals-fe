import React from "react";

const Profile = () => {
  return (
    <div>
      <h2>Welcome Back {/* insert user here */}!</h2>
      <button>New Goal</button>
    </div>
  );
};

export default Profile;
